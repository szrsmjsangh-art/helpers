export default function ContactPicker({ onPick }) { const supported = typeof navigator !== 'undefined' && !!navigator.contacts?.select; const pick=async()=>{ if(!supported) return; const contacts=await navigator.contacts.select(['name','tel'],{multiple:true}); onPick?.(contacts); }; return <button type="button" onClick={pick} disabled={!supported} className="rounded-xl border px-4 py-3 disabled:opacity-50">{supported ? 'Select contacts' : 'Contact picker not supported'}</button> }
