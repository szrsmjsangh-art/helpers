import { useEffect, useState } from 'react'
import PageHeader from '../components/layout/PageHeader'
import TextInput from '../components/common/TextInput'
import PhoneInput from '../components/common/PhoneInput'
import TextArea from '../components/common/TextArea'
import SelectInput from '../components/common/SelectInput'
import Button from '../components/common/Button'
import ErrorMessage from '../components/common/ErrorMessage'
import AddHelpersSuccess from '../components/add-helper/AddHelpersSuccess'
import ContactPicker from '../components/add-helper/ContactPicker'
import { getCategories, getServiceTypes } from '../services/categoryService'
import { submitHelper } from '../services/submissionService'
export default function AddHelperPage(){ const [categories,setCategories]=useState([]); const [types,setTypes]=useState([]); const [success,setSuccess]=useState(false); const [error,setError]=useState(''); const [form,setForm]=useState({name:'',mobile:'',whatsapp:'',categoryId:'',serviceTypeId:'',area:'',description:'',submittedName:''}); useEffect(()=>{getCategories().then(setCategories)},[]); useEffect(()=>{ if(form.categoryId) getServiceTypes(form.categoryId).then(setTypes); else setTypes([])},[form.categoryId]); const submit=async(e)=>{e.preventDefault();setError('');setSuccess(false);try{await submitHelper(form);setSuccess(true);setForm({name:'',mobile:'',whatsapp:'',categoryId:'',serviceTypeId:'',area:'',description:'',submittedName:''})}catch(err){setError(err.message)}}; const picked=(contacts)=>{const c=contacts?.[0]; if(c) setForm(f=>({...f,name:c.name?.[0]||'',mobile:c.tel?.[0]||''}))}; return <div><PageHeader title="Add Helper" subtitle="Submit a useful service contact to the community directory."/><div className="mb-4"><ContactPicker onPick={picked}/></div>{success && <div className="mb-4"><AddHelpersSuccess/></div>}<form onSubmit={submit} className="space-y-4"><TextInput label="Helper Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><PhoneInput label="Mobile" value={form.mobile} onChange={e=>setForm({...form,mobile:e.target.value})} required/><PhoneInput label="WhatsApp" value={form.whatsapp} onChange={e=>setForm({...form,whatsapp:e.target.value})}/><SelectInput label="Category" value={form.categoryId} onChange={e=>setForm({...form,categoryId:e.target.value,serviceTypeId:''})} required><option value="">Select category</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name_en}</option>)}</SelectInput><SelectInput label="Service Type" value={form.serviceTypeId} onChange={e=>setForm({...form,serviceTypeId:e.target.value})} required><option value="">Select service</option>{types.map(t=><option key={t.id} value={t.id}>{t.name_en}</option>)}</SelectInput><TextInput label="Area" value={form.area} onChange={e=>setForm({...form,area:e.target.value})} required/><TextArea label="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} rows={3}/><TextInput label="Your Name (optional)" value={form.submittedName} onChange={e=>setForm({...form,submittedName:e.target.value})}/><ErrorMessage message={error}/><Button type="submit" className="w-full">Submit Helper</Button></form></div> }
